document.addEventListener("DOMContentLoaded", function () {
    const cameraCount = document.querySelector(".stats .number");
    
    // Example: Fetch data from an API later
    cameraCount.textContent = "3"; // Change the number dynamically
});
